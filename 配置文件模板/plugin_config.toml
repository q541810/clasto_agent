# ==========================================
# 插件系统配置模板
# 功能：插件系统全局配置
# ==========================================

# 是否启用插件系统
enable = true

# 插件目录（相对于项目根目录）
plugin_dir = "插件"

# 插件配置覆写目录（相对于项目根目录）
plugin_config_dir = "插件配置"